<div class="main-container">
  <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= cl_image_path activity_image_key(@activity) %>)">
    <div class="container">
    </div>
  </div>


  <div class="container">
    <div  class="border-bottom border-dark flexbox mt-5 pt-5" style="align-items:flex-start">
      <div class="trainer-box">
     
       
        <p class="fas fa-check-square color-grey">
          <%= @activity.category%></p>
          <br>
        <p class="fas fa-clock color-grey">
          <%= @activity.duration%> minutes</p>
      
    
      <div class="d-flex justify-content-between mt-5 pt-3">
        <p><%= link_to "edit", edit_trainers_activity_path(@activity), class: "btn btn-primary" %>
    
      
        <%= link_to 'Destroy',  trainers_activity_path(@activity), class: "btn btn-info", method: :delete, data: { confirm: "Are you sure?" } %></p>
        
        </div> 
      </div>
          <p><div><i class="fas fa-calendar-check"> Class schedule:</i>

          <p class="color-grey" id="schedules">
          <% @schedules.each do |schedule| %>
            <%= schedule.date_formatted %></p>
            <p><% end %></p>
            </p>
            
        </div>

    </div>
  </div>
     
  <div class="container">
      <%= simple_form_for [:trainers, @activity, @schedule] do |f| %>
      <%= f.error_notification %>
      <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

      <div class="form-inputs mt-2 pt-4">
        <%= f.input :date, as: :date, html5: true%>
        <%= f.input :time, as: :time, html5: true%>
      </div>

    <div class="form-actions mt-4 pt-5">
      <%= f.button :submit, class: "btn btn-primary"  %> 
    </div>
    <p><% end %></p>

 
   <p><div class="d-flex justify-content-between"><%= link_to "View All Classes", trainers_dashboards_path, class: "btn btn-primary" %>
   <%= link_to "Back", employees_activities_path, class: "btn btn-primary" %></div></p>
</div>
  
